
general {
    hide_cursor = true          # Hide mouse cursor on lock screen
    grace = 3                  # Seconds to unlock on mouse movement after authentication
    disable_loading_bar = false # Show loading bar on start
    no_fade_in = false          # Enable fade-in animation
    no_fade_out = false         # Enable fade-out animation
    ignore_empty_input = true   # Don't validate empty password input
    immediate_render = false    # Render UI immediately on start
    fractional_scaling = 2      # Auto fractional scaling support: 0=off, 1=on, 2=auto
    pam_module = "hyprlock"     # PAM module to use for authentication
    enable_fingerprint = false  # Enable fingerprint authentication
    fingerprint_ready_message = "(Scan fingerprint to unlock)"
    fingerprint_present_message = "Scanning fingerprint"
}

# # DATE
# label {
#   monitor =
#   text = cmd[update:1000] echo "$(date +"%A, %B %d")"
#   color = rgba(242, 243, 244, 0.75)
#   font_size = 22
#   font_family = JetBrains Mono
#   position = 0, 300
#   halign = center
#   valign = center
# }
#
# # TIME
# label {
#   monitor = 
#   text = cmd[update:1000] echo "$(date +"%-I:%M")"
#   color = rgba(242, 243, 244, 0.75)
#   font_size = 95
#   font_family = JetBrains Mono Extrabold
#   position = 0, 200
#   halign = center
#   valign = center
# }

background {
  # blur = true
  dim = 0.25
  # monitor empty = all monitors
  monitor =
  #path = screenshot
  #path = ~/Pictures/wallpaper/HeartSutra.png
  path = 
  color = rgba(0,0,0,0)
  #blur_passes = 2
  #contrast = 1
  brightness = 0.3
  vibrancy = 0.2
  vibrancy_darkness = 0.2
}

# INPUT FIELD
input-field {
  monitor =
  size = 250, 60
  outline_thickness = 2
  dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
  dots_spacing = 0.35 # Scale of dots' absolute size, 0.0 - 1.0
  dots_center = true
  outer_color = rgba(0, 0, 0, 0)
  inner_color = rgba(0, 0, 0, 0.2)
  font_color = rgba(0,200,0,0.2)
  fade_on_empty = false
  rounding = -1
  check_color = rgb(204, 136, 34)
  #placeholder_text = <i><span foreground="##00ad6f">Input Password...</span></i>
  hide_input = true
  position = 0, -200
  halign = center
  valign = center
}

# Password input widget (text field)
password_input {
    font = "monospace"
    font_size = 20
    color = "#ffffff"
    background = "rgba(0, 0, 0, 0.75)"
    border_color = "#333333"
    border_thickness = 2
    padding = 10
    max_length = 64
    placeholder = "Enter password"
}

# Text widget for status messages like authentication failure or status
status_text {
    font = "monospace"
    font_size = 14
    color = "#ff5555"
    position = "bottom_center"
    height = "20"
}

# Optional lock icon or logo widget (can specify an image path or unicode icon)
lock_icon {
    icon = "🔒"
    font_size = 40
    color = "#eeeeee"
    position = "center"
    padding = 20
}
