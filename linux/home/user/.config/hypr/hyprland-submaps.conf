# submap: main {{{
submap = main

bind = , L, submap, layout
  # submap: main>layout {{{
  submap = layout

  bind = , M, layoutmsg, master
  bind = , D, layoutmsg, dwindle

  bind = , catchall, submap, reset
  bind = , escape, submap, reset
  # }}} submap: main>layout
submap = main

bind = , W, submap, window
  # submap: main>window {{{
  submap = window

  bind = , R, submap, window_resize
    # submap: main>window>resize {{{
    submap = window_resize

    binde = , right, resizeactive, 10 0
    binde = , left, resizeactive, -10 0
    binde = , up, resizeactive, 0 -10
    binde = , down, resizeactive, 0 10

    bind = , catchall, submap, reset
    bind = , escape, submap, reset
    # }}} submap: main>window>resize
  submap = window

  bind = , catchall, submap, reset
  bind = , escape, submap, reset
  # }}} submap: main>window
submap = main

bind = , S, submap, system
  # submap: main>system {{{
  submap = system

  bind = , Q, exit,
  bind = , H, exec, sudo /usr/bin/systemctl hibernate
  bind = , P, exec, sudo /usr/bin/systemctl suspend

  bind = , catchall, submap, reset
  bind = , escape, submap, reset
  # }}} submap: main>system
submap = main


bind = , catchall, submap, reset
bind = , escape, submap, reset
# }}} submap: main
submap = reset
